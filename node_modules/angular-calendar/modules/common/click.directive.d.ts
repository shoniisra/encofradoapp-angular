import { Renderer2, ElementRef, OnInit, OnDestroy, EventEmitter, NgZone } from '@angular/core';
export declare class ClickDirective implements OnInit, OnDestroy {
    private renderer;
    private elm;
    private document;
    private zone;
    clickListenerDisabled: boolean;
    click: EventEmitter<MouseEvent>;
    private destroy$;
    constructor(renderer: Renderer2, elm: ElementRef<HTMLElement>, document: any, zone: NgZone);
    ngOnInit(): void;
    ngOnDestroy(): void;
    private listen;
}
